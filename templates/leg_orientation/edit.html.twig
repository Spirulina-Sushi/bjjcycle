{% extends 'base.html.twig' %}

{% block title %}Edit LegOrientation{% endblock %}

{% block body %}
    <h1>Edit LegOrientation</h1>

    {{ include('leg_orientation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('leg_orientation_index') }}">back to list</a>

    {{ include('leg_orientation/_delete_form.html.twig') }}
{% endblock %}
