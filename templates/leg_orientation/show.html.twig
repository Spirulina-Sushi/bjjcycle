{% extends 'base.html.twig' %}

{% block title %}LegOrientation{% endblock %}

{% block body %}
    <h1>LegOrientation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leg_orientation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ leg_orientation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('leg_orientation_index') }}">back to list</a>

    <a href="{{ path('leg_orientation_edit', {'id': leg_orientation.id}) }}">edit</a>

    {{ include('leg_orientation/_delete_form.html.twig') }}
{% endblock %}
