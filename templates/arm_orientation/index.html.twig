{% extends 'base.html.twig' %}

{% block title %}ArmOrientation index{% endblock %}

{% block body %}
    <h1>ArmOrientation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for arm_orientation in arm_orientations %}
            <tr>
                <td>{{ arm_orientation.id }}</td>
                <td>{{ arm_orientation.name }}</td>
                <td>
                    <a href="{{ path('arm_orientation_show', {'id': arm_orientation.id}) }}">show</a>
                    <a href="{{ path('arm_orientation_edit', {'id': arm_orientation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('arm_orientation_new') }}">Create new</a>
{% endblock %}
