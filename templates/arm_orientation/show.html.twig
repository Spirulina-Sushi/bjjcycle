{% extends 'base.html.twig' %}

{% block title %}ArmOrientation{% endblock %}

{% block body %}
    <h1>ArmOrientation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arm_orientation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ arm_orientation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('arm_orientation_index') }}">back to list</a>

    <a href="{{ path('arm_orientation_edit', {'id': arm_orientation.id}) }}">edit</a>

    {{ include('arm_orientation/_delete_form.html.twig') }}
{% endblock %}
