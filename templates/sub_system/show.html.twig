{% extends 'base.html.twig' %}

{% block title %}SubSystem{% endblock %}

{% block body %}
    <h1>SubSystem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_system.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_system.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_system_index') }}">back to list</a>

    <a href="{{ path('sub_system_edit', {'id': sub_system.id}) }}">edit</a>

    {{ include('sub_system/_delete_form.html.twig') }}
{% endblock %}
